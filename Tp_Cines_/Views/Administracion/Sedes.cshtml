@using CapaServicio;
@model  IEnumerable<Sedes>
@{
    ViewBag.Title = "Gestión de Sedes";
    Layout = "~/Views/Administracion/_LayoutAdministrador.cshtml";
}

<h2>Sedes</h2>

<section id="three" class="wrapper style1 fade-up">
    <div class="inner">
        <h2>Películas vigentes</h2>

        <table>
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th>Dirección</th>
                    <th>Precio Entrada General</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null)
                {
                foreach (var s in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => s.Nombre)
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => s.Direccion)
                        </td>

                        <td>
                            @Html.DisplayFor(modelItem => s.PrecioGeneral)
                        </td>
                        <td>
                            @Html.ActionLink("Editar", "CrearSede",  new { @id = s.IdSede } )
                        </td>

                    </tr>

                }
                }

            </tbody>

        </table>

        <ul class="actions fit">
            <li class="button special fit">
                @Html.ActionLink("Crear nuevo", "CrearSede")
            </li>
        </ul>
    </div>
</section>