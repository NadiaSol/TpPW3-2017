@model List<SelectListItem>

@*@{
    ViewBag.Title = "Dias";


}*@

    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jquery/1.3.2/jquery.min.js"></script>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jqueryui/1.7.2/jquery-ui.min.js"></script>
    <link type="text/css" href="/Content/jquery-ui-1.7.2.custom.css" rel="stylesheet" />

    <script type="text/javascript">
        jQuery(function ($) {
        $("#datepicker").datepicker({ showOn: 'button',
            buttonImage: '/img/datapicker.png',
            //buttonImageOnly: true,
            beforeShowDay: function(date){ return [date.getDay() == 1 || date.getDay() == 2 || date.getDay() == 4 || date.getDay() == 5,""]} });
        });

    </script>

    <div id="wrapper">
        <section id="main" class="wrapper style1 fade-up">
            <div class="inner">


                @*            @using (Html.BeginForm("Hora", "Peliculas", FormMethod.Post))
            {
                <label>Dias</label>
                @Html.DropDownList("lis", "Elegir dia")

                //  @Html.DropDownListFor(m => m. , new SelectList(ViewBag.lis, "IdPelicula"),"Seleccionar")

     
                <input type="submit" name="enviar" value="Enviar" />
            }*@
                
                <form action="/Peliculas/Hora" name="formu" method="post">
                    @Html.ValidationSummary(false, "", new { @class = "text-danger" })
                    <fieldset>
                        <legend>Seleccionar</legend>
                        <div>
                            <label>Fecha</label>
                            <input type="text" id="datepicker" name="dia" value="" />
                            <input type="submit" name="enviar" value="Enviar" />
                        </div>
                    </fieldset>
                </form>

            </div>
        </section>
    </div>